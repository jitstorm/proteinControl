# ProteinControl 串口协议说明

## 通信与帧格式

主控使用 USART2，参数为 `9600, 8N1`，串口工具必须使用 HEX 发送。USART1 仅接收外部温度板数据；USART3 已禁用，PB10/PB11 用于 DMA 步进脉冲。

所有主控请求和应答均固定为 10 字节：

```text
AA CMD D0 D1 D2 D3 D4 D5 55 SUM
```

校验和为 `AA` 至 `D5` 共 8 字节累加后的低 8 位，`0x55` 不参与累加：

```text
SUM = (AA + CMD + D0 + D1 + D2 + D3 + D4 + D5) & 0xFF
```

接收端仅在帧头为 `AA`、第 9 字节为 `55` 且校验正确时执行命令。未使用的数据字节必须填 `00`。

## 当前有效命令

| CMD | 功能 | 应答 |
| --- | --- | --- |
| `00` | 保留空操作 | 无应答 |
| `01` | 读取 74HC165 输入 | `D0..D1` 为 16 路输入快照，`D2..D5=00` |
| `05` | 19 路单向电机立即控制 | 回显请求字段 |
| `06` | 19 路单向电机定时运行 | `D0` 回显电机编号，`D1` 回显时间，`D2` 为结果码 |
| `07` | 19 路单向电机定时加传感器停止 | `D0` 为结果码，`D1` 为电机编号 |
| `08` | 查询 19 路单向电机运行状态 | `D0..D2` 为运行位图 |
| `09` | 五路正反转电机立即控制 | 回显请求字段 |
| `0A` | 五路正反转电机定时运行 | `D0` 为结果码，`D1` 为电机编号 |
| `0B` | 五路正反转电机定时加传感器停止 | `D0` 为结果码，`D1` 为电机编号 |
| `0C` | 查询五路正反转电机状态 | 返回状态和停止原因 |
| `1B` | X 轴步进电机（PB11） | 原样回显 |
| `1C` | Y 轴步进电机（PB10） | 原样回显 |
| `1E` | PU3 步进电机（PB13） | 原样回显 |
| `1F` | 查询 Y 轴步进状态 | `D0=0` 运行中，`D0=1` 已停止/完成 |
| `20` | Y 轴步进电机带传感器停止 | 原样回显；传感器触发后主动上报 `1F` |

`CMD 0x02` 已移除。未列出的历史命令当前不支持，收到后以该命令号应答，`D0=05`。

## 结果码

`06` 的应答为 `D0=电机编号`、`D1=时间(100 ms 单位)`、`D2=结果码`、`D3..D5=00`。`07`、`0A`、`0B` 的常规应答中，`D0` 为结果码，`D1` 回显请求的电机编号，`D2..D5=00`。`05`、`09` 保持回显请求字段：

| 结果码 | 含义 |
| --- | --- |
| `00` | 执行成功 |
| `01` | 电机编号非法 |
| `02` | 动作、保留字节或查询参数非法 |
| `03` | 传感器编号非法 |
| `04` | 时间参数非法 |
| `05` | 命令暂不支持 |

## 19 路单向电机

### `05`：立即控制

```text
AA 05 MOTOR ACTION 00 00 00 00 55 SUM
```

`MOTOR` 范围为 `01..13`（十进制 1..19）；`ACTION=00` 停止，`ACTION=01` 启动。新命令只覆盖该电机的已有定时或传感器任务，不影响其他电机。

### `06`：定时运行

```text
AA 06 MOTOR TIME 00 00 00 00 55 SUM
```

`TIME` 的单位为 100 ms，必须大于 0。接收成功后立即启动，到时自动停止。回包格式为 `AA 06 MOTOR TIME RESULT 00 00 00 55 SUM`。

### `07`：传感器或超时停止

```text
AA 07 MOTOR SENSOR LEVEL TIME100_L TIME100_H 00 55 SUM
```

`SENSOR` 范围 `01..10`（十进制 1..16）；`LEVEL=00` 为低电平触发，`LEVEL=01` 为高电平触发。超时值为小端 16 位、单位 100 ms，必须大于 0。传感器和超时以先发生者为准；传感器采用 30 ms 连续稳定防抖。

### `08`：查询状态

请求：

```text
AA 08 00 00 00 00 00 00 55 SUM
```

回复：

```text
AA 08 MAP0 MAP1 MAP2 00 00 00 55 SUM
```

位图中 `1` 表示对应电机正在输出运行：`MAP0 bit0..7` 对应电机 1..8，`MAP1 bit0..7` 对应电机 9..16，`MAP2 bit0..2` 对应电机 17..19。

单向电机输出映射如下：

| 电机编号 | 74HC595 输出位 |
| --- | --- |
| 1..8 | `HC595Data[3]` bit0..7 |
| 9..12 | `HC595Data[2]` bit0..3 |
| 13..19 | `HC595Data[0]` bit1..7 |

## 五路正反转电机

电机编号为 `01`~`05`。方向值：`00` 停止、`01` 正转、`02` 反转。每路换向均先关闭输出并保持 20 ms 死区，再输出新方向，防止正反转同时导通。

硬件映射：电机 1 使用 `HC595Data[1] bit0=A、bit1=B`；电机 2 使用 `bit2=A、bit3=B`；电机 3 使用 `PC14/PC15/PC0`，电机 4 使用 `PC1/PC2/PC3`，电机 5 使用 `PC5/PB0/PB1`（依次为 IN1/IN2/EN）。`00` 为停止，`10` 为正转，`01` 为反转，程序不会输出 `11`。

### `09`：立即控制

```text
AA 09 MOTOR ACTION 00 00 00 00 55 SUM
```

### `0A`：定时运行

```text
AA 0A MOTOR DIR TIME_L TIME_H TIME_HH TIME_HHH 55 SUM
```

`DIR` 只能为 `01` 或 `02`。时间为小端 32 位毫秒值且必须大于 0；计时从死区结束、实际输出新方向后开始。

### `0B`：传感器或超时停止

```text
AA 0B MOTOR DIR SENSOR LEVEL TIME100_L TIME100_H 55 SUM
```

`SENSOR` 范围为 1..16，`LEVEL=00/01` 分别为低/高电平触发；超时为小端 16 位、单位 100 ms，必须大于 0。传感器采用 30 ms 防抖，和超时同样以先发生者为准。

### `0C`：查询状态

请求：

```text
AA 0C 00 00 00 00 00 00 55 SUM
```

回复中 `D0`、`D1` 依次为电机 1、2 状态：`00` 停止、`01` 正转、`02` 反转、`03` 换向死区。`D2`、`D3` 为对应停止原因：`00` 无、`01` 超时、`02` 传感器触发、`03` 被新命令覆盖、`04` 参数错误；`D4` 为电机 3 状态，`D5` 低四位为电机 4 状态、高四位为电机 5 状态。

## 步进电机命令

### `1B`：X 轴 PB11

```text
AA 1B STEP_H STEP_L DIR SPEED 00 00 55 SUM
```

### `1C`：Y 轴 PB10

```text
AA 1C STEP_H STEP_L DIR SPEED 00 00 55 SUM
```

### `1E`：PU3 轴 PB13

```text
AA 1E STEP_H STEP_L DIR SPEED 00 00 55 SUM
```

`STEP_H STEP_L` 为大端 16 位步数，`DIR` 仅使用 bit0。PU3 的起步速度为 `500 steps/s`、最大速度为 `SPEED * 200 steps/s`、加速度为 `100000 steps/s²`。STEP 由 TIM7 更新事件触发 DMA2 Channel4 向 `GPIOB->BSRR` 交替写 PB13 的置位和复位值，两个 DMA edge 构成一个实际步数；DIR 为 U86（第二片 74HC595）Q6、软件 bit6。

`STEP_H STEP_L` 为大端 16 位步数；`DIR` 仅使用 bit0；最大速度为 `SPEED * 200 steps/s`，起步速度为 `500 steps/s`，加速度为 `100000 steps/s²`。X 轴方向使用 `HC595Data[1] bit4`（DIR1），Y 轴方向使用 bit5（DIR2）；bit6、bit7 保留给 DIR3、DIR4，不占用单向电机所在的 `HC595Data[2]`。

### `20`：Y 轴带传感器停止

```text
AA 20 STEP_H STEP_L DIR SPEED SENSOR LEVEL 55 SUM
```

前四个数据字节与 `1C` 一致。`SENSOR` 范围为 1..16；`LEVEL=00` 为低电平触发，非 0 为高电平触发。传感器参数非法时不启动，回显帧中 `SENSOR` 被置为 `00`。传感器触发后，设备停止 Y 轴并主动发送：

```text
AA 1F 01 01 SENSOR 00 00 00 55 SUM
```

其中 `D1=01` 表示传感器触发停止，`D2` 为触发传感器编号。
