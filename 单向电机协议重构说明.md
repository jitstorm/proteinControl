# 单向电机协议重构说明

## 帧格式

所有串口命令均为固定 10 字节：

```text
AA CMD D0 D1 D2 D3 D4 D5 55 SUM
```

`SUM` 为 `AA` 至 `D5` 共 8 字节累加后的低 8 位。

## 单向电机映射

外部电机编号为 1 至 19，软件内部下标为编号减 1：

| 电机编号 | 74HC595 输出位 |
| --- | --- |
| 1 至 8 | `HC595Data[3]` bit0 至 bit7 |
| 9 至 12 | `HC595Data[2]` bit0 至 bit3 |
| 13 至 19 | `HC595Data[0]` bit1 至 bit7 |

`HC595Data[1]` 为步进电机相关控制专用，不属于单向电机输出。其后四位按原理图 U86 分配为 `bit4=DIR1`、`bit5=DIR2`、`bit6=DIR3`、`bit7=DIR4`。

## 硬件风险提示

当前生效的 X、Y 步进方向控制分别使用 `HC595Data[1]` bit4（DIR1）和 bit5（DIR2），不再占用 `HC595Data[2]` bit0 至 bit3；因此不会与单向电机 9 至 12 的输出位冲突。`HC595Data[1]` bit6、bit7 保留给后续 DIR3、DIR4 使用。

历史实现中仍保留了被 `#if 0` 注释的 `HC595Data[2]` 方向位写入代码，仅供追溯，未参与编译。步进电机的速度、DMA、定时和完成反馈逻辑保持不变。
